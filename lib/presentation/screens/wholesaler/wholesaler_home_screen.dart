import 'package:flutter/material.dart';

class WholesalerHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Wholesaler Dashboard'),
      ),
      body: Center(
        child: Text('Welcome, Wholesaler!'),
      ),
    );
  }
}
