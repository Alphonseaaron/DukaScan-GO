import 'package:flutter/material.dart';

class WholesalerPosAddProductScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Product to POS'),
      ),
      body: Center(
        child: Text('Add Product to POS Screen'),
      ),
    );
  }
}
