import 'package:flutter/material.dart';

class GigDiscoveryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gig Discovery'),
      ),
      body: Center(
        child: Text('Find gigs here.'),
      ),
    );
  }
}
